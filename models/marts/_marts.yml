models:
  - name: fct_public__price_over_time
    config:
      group: word_count_team
      materialized: table
      
  - name: dim_public__card_info_image
    config:
      materialized: table  
  
# deprecated below


  - name: dim_public__most_expensive_cards
    config:
      group: word_count_team
  
  - name: dim_public__setcardcount
    config:
      group: word_count_team
  
  - name: fct_public__yearcompare
    config:
      group: word_count_team

  - name: dim_public__wordcount
    description: ""
    config:
      group: word_count_team
      access: private
    columns:
      - name: card_name
        data_type: varchar
        description: ""

      - name: word_count
        data_type: number
        description: ""

      - name: card_text
        data_type: varchar
        description: ""

      - name: uri
        data_type: varchar
        description: ""